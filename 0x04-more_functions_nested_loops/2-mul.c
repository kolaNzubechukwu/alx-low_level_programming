#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: stores first intiger
 * @b: stores second int
 * Return: returns result answer
 *
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
